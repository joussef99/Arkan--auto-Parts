# Server Configuration
PORT=5000
NODE_ENV=development
DB_PATH=arkan_parts.db

# Required in production
JWT_SECRET=replace-with-long-random-secret

# Optional auth/session tuning
JWT_EXPIRES_IN=8h

# Set to 0 in production unless explicitly bootstrapping a new instance
ENABLE_SEED_DEFAULT_CREDENTIALS=1